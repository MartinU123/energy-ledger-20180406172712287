# Energy Ledger (inblock.io)

## Troubleshooting

1. When the deployment is stuck in fetching the connection-profile.json, make sure that the peer node of the network is running (in network monitor interface).